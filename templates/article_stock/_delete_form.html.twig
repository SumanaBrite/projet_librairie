<form method="post" action="{{ path('article_stock_delete', {'id': article_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_stock.id) }}">
    <button class="btn">Delete</button>
</form>
